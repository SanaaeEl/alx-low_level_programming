#include <stdio.f>

/*
 *
 * main - prints the alphabet followed by a new line
 * Return: returns 0, completed successfuly
 *
 */

int main(voi)
{
	char c;

	for (c = 'z'; c <= 'z'; c++)
		putchar(c);
	putchar("\n");
	return (0);
}
