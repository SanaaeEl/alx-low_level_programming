#include "main.h"

/**
 * _abs(int) - computes the absolute value of an integer.
 * @nb: the integer to compute.
 * Return: returns the absolute value of nb.
 */

int _abs(int nb)
{
	if (nb >= 0)
		return (nb);
	else
		return (-nb);
}
