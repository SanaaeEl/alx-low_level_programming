#include "main.h"

/**
 * add - adds two integers and returns the result.
 * Return: returns tha addition of two numbers.
 *@a: first number.
 *@b: second number.
 */

int times_table(int a, int b)
{
	return (a+b);
}
